# Copy this file to .env and fill in your API key.
# The app defaults to Claude. Set LLM_PROVIDER=openai to use GPT-4o instead.

LLM_PROVIDER=claude

# Anthropic (Claude) — get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI — only needed if LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...

# Path to the folder containing the PDFs (already set to the right default)
DOCS_PATH=/Users/rehaananjaria/Visic/CouncilFiles/17-0090-S1
